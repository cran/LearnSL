#' @title Test Database 1
#'
#' @format ## `db1rl`
#' A data frame with 4 independent variables (first 4 columns, representing different line types).
#' The last column is the independent variable.
#'
#'
"db1rl"

#' @title Test Database 2
#'
#' @format ## `db_per_and`
#' A data frame with 3 independent variables (first 3 columns) and one independent variable (last column).
#' It represents a 3 input "AND" logic gate.
#'
"db_per_and"

#' @title Test Database 3
#'
#' @format ## `db_per_or`
#' A data frame with 3 independent variables (first 3 columns) and one independent variable (last column).
#' It represents a 3 input "OR" logic gate.
#'
"db_per_or"

#' @title Test Database 4
#'
#' @format ## `db_per_xor`
#' A data frame with 3 independent variables (first 3 columns) and one independent variable (last column).
#' It represents a 3 input "XOR" logic gate.
#'
"db_per_xor"

#' @title Test Database 5
#'
#' @format ## `db_flowers`
#' A data frame representing features of flowers. It has 4 independent variables (first 4 columns) and one independent variable (last column).
#'
"db_flowers"

#' @title Test Database 6
#'
#' @format ## `db2`
#' A data frame with 3 independent variables (first 3 columns) and one independent variable (last column).
#' It has information about vehicles.
#'
"db2"

#' @title Test Database 7
#'
#' @format ## `db3`
#' A data frame with 3 independent variables (first 3 columns) and one independent variable (last column).
#' It has information about vehicles. Similar to db2 but a little bit more complex.
#'
"db3"

#' @title Test Database 8
#'
#' @format ## `db_tree_struct`
#' Decision tree structure. output of the decision_tree() function "decision_tree(db2, "VehicleType", 4, "gini")"
#'
"db_tree_struct"
